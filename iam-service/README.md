# IAM Service – placeholder

The supplied HLD assigns authentication and session management to User Management. Authentication/authorization will therefore be implemented in `user-service`. This placeholder can be removed from the final repository.
